# data/

실제 데이터 파일은 용량 문제로 GitHub에 포함되지 않습니다.

## 필요한 파일

| 파일명 | 설명 | 크기 (참고) |
|--------|------|------------|
| `offline_output.json` | Module 2 Retrieval 출력 (graph 파트 입력) | ~1.9 GB |
| `citation_graph.zip` | citation graph — 내부에 `citation_graph.pkl` 포함 (`forward`/`backward` dict) | ~143 MB |
